# Contributing

Timeline: docs/typo PRs welcome from Drop 2; new tasks/robots from Season 3 (a contributor guide ships then); chapter prose is never accepted externally — zero2robot is single-voice by design.

Before any PR: run `make check`. Curriculum code PRs face the strictest review — expect your beautiful abstraction to be rejected for being an abstraction.
