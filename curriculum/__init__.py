"""zero2robot curriculum package.

Chapter artifacts are run as standalone files, never imported. This
__init__.py exists only so chapters (and tests) can import the shared
utilities as `curriculum.common.*` after putting the repo root on sys.path.
"""
