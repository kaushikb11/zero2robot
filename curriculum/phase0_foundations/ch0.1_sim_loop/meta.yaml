id: ch0.1-sim-loop
title: "The Simulation Loop"
artifact: sim_loop.py       # ~160 LOC, CPU-only (hook cap 450)
task: empty-scene-pusher    # inline MJCF: plane + free box + one slide-actuated pusher (scene authorship is ch0.2)
objectives:
  - Hold the mjModel/mjData distinction — the compiled world that never changes vs the state that changes every step
  - Step a simulation with mj_step and read qpos/qvel, including why a free joint has 7 position numbers but 6 velocities
  - Perturb a running simulation with xfrc_applied and watch physics respond (and learn that the force persists until cleared)
  - Record a run to a .rrd file and scrub it in rerun on the sim_time timeline
scale_lab: false
read_the_real_thing: false
demo: sim-loop-perturb

# Reference metrics + seeded bands for the exercise checks (exercise-spec:
# "seeded-run band with provenance", not a bare magic number). checks.py reads
# these instead of hardcoding tolerances.
exercise_checks:
  ex2:                    # ex2_bughunt_sticky_shove.py, run with --smoke --seed 0
    provenance: "seed 0 on cpu, mujoco 3.10.0 / numpy 2.4.6; --smoke (300 steps); measured 2026-07-03"
    buggy_lateral_drift: 0.418   # smoke signature of the injected sticky-shove bug (~0.42 m vs ~0.09 m)
    buggy_band: 0.05             # |drift - buggy| < band => bug still present, SKIP
    ref_lateral_drift_abs: 0.01  # a correct fix reproduces sim_loop.py within these bands
    ref_box_final_pos_abs: 0.01

notebook_hash: 0548d5a105de2aa70d656547ff0286e68d18c6b3b6b88efca780eef7766b6845        # sha256 of the artifact at generation; drift => regenerate (notebook-tier-test)
notebook_file_hash: 8ca5a8f586c58bc184fdc3a8ac74445659298e84e4d400565269ce5ab430b015   # sha256 of the generated notebook bytes; mismatch => hand-edited

region_hashes:
  setup: 3c978285295525de8befe41d263f323f73d8cdbd0ea9c599b70376275d29831c
  scene: e1cc421e0b2b5902a08f0b946a28511e14e1d60c4f23635faa7c66738f22f9c2
  perturb: 072ff321d9fedad038ffed70d20e49e462d90390ff47ddfd391e73c82c25f063
  loop: 3c36a8c2ae2471f7bbd6e8d1ef6a67aa9a3cf728e9f80cdb5425eb2880618615
  inspect: e6933c737f63d79bc3e3a41533f9b96d8d2ece2cc87dc88a455ecaa0a7f0c4c7
