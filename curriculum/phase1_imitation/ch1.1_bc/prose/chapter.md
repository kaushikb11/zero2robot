# 1.1: Behavior Cloning: The Dumbest Thing That Works

<!-- objectives: rendered from meta.yaml, do not duplicate here -->

## See it work
<!-- The hook. Embedded live demo of the END STATE of this chapter (demo/embed.yaml).
     1-2 paragraphs: what the learner is looking at, what to try with their mouse,
     and the one observable behavior this chapter explains. -->

## The problem
<!-- Why last chapter's approach breaks on this chapter's task. Show the failure
     (embedded rerun trace or demo of the old policy failing), then diagnose it.
     Ends with a one-sentence statement of what we'll build. -->

## Build
<!-- Region-by-region walkthrough in dependency order. Each region:
     one orienting sentence → include-by-region code block → prose that earns it.
     Subheadings mirror region names: ### Setup, ### Data, ### Model, ### Train, ### Eval -->

## Run it
<!-- The exact command(s). The honest wall-clock table (auto-rendered).
     What healthy training looks like in rerun (blueprint screenshot),
     and the first thing to check if it doesn't look like that. -->

## Break it
<!-- One deliberate failure: the command that induces it, the rerun trace of the
     failure signature, the diagnosis walk. This is where debugging skill is taught. -->

## Exercises
<!-- Rendered from exercises/. One line of framing only. -->

## What's next
<!-- One paragraph. Name the limitation that survives this chapter and
     let it set up the next chapter's "The problem". -->
